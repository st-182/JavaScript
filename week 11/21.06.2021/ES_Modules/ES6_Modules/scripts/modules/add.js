// ES6 Modules
// --adding export
// ---default export - could be only one
// ---name export - could be used ulimited numbers of times
// export default addTwoNumbers = (a, b) => a + b;
// arba:
const addNumbers = (a, b) => a + b;
export default addNumbers;

export const addThreeNumbers = (a, b, c) => a + b + c;
export const addFourNumbers = (a, b, c, d) => a + b + c + d;
// export addTreeNumbers;
